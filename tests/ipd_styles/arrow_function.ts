class A {
    public doSomething() {
        const Test = () => {
            // Do something here
        }
        const getNodeKey = ({ node, treeIndex }) => treeIndex
const {a,b} = c
    }
private aProp = (a) => (a+1) 
private bProp = (a) => (a+1);
private cProp = (a) => a
private dProp = (a) => { return a}

    private doPrivate = () => {
        const x = 1
    }
    
    public doPublic = () => {
        const y = 2
    }

    private priv: string
    public publ : string
}